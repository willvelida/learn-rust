# Before you move on....

* The game compiles and runs with `cargo run`
* Unrecognized input asks the player to try again instead of crashing
* The loop ends when the player types quit, and prints a final score
* You can explain why the moves are an enum and why parse returns an `Option`

If you want to take it further, add a best-of-five mode that stops after one player reaches three wins, or add lizard and spock for the five-move variant.