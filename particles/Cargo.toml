[package]
name = "particles"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2.126"

# No prebuilt wasm-opt binary exists for Windows ARM64 (aarch64), so disable it.
[package.metadata.wasm-pack.profile.release]
wasm-opt = false
