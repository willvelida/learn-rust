# Before you move on....

Make sure you can do all of these:

* `rustc`, `cargo`, and `rustup` all report a version
* A standalone `main.rs` compiles with `rustc` and runs
* A Cargo project runs with `cargo run`
* You can explain what `cargo check` does differently from `cargo build`