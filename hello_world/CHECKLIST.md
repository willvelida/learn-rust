# What we covered

1. Installed Rust with rustup
2. Made sure they work
3. Compiled a file by hand to see what the complier does.
4. Learned the 4 Cargo command we'll lean on the most:

```bash
cargo run     # compile and run
cargo build   # compile only (debug)
cargo check   # type-check without producing a binary (fast)
cargo build --release  # optimized build
```